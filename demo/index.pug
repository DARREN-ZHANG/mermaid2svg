doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Mermaid → SVG
    link(rel="stylesheet" href="https://registry.npmmirror.com/18s/0.2.24/files/_.css")
    link(rel="stylesheet" href="./style.css")
  body
    c-vs
      header
        .header-controls
          c-i18n
          a.BtnC.github-link(href="https://github.com/webc-site/math" target="_blank" aria-label="GitHub Repository")
            i.Ico
              include svg/github.svg
      main
        .container
          .header-main
            h1
              // TODO(i18n-loop): extract to key
              a#ui-title(href="https://github.com/webc-site/math" target="_blank") Mermaid → SVG
            // TODO(i18n-loop): extract to key
            p#ui-subtitle.subtitle Browser-side Mermaid to SVG converter
          .main-grid
            .card.usage-card.Lg
              .card-header
                // TODO(i18n-loop): extract to key
                h2#ui-usage-title Usage
              pre.code-block
                code#ui-usage-code

            .card.benchmark-card.Lg
              // TODO(size-loop): wire size/gzip comparison SVG bar chart here
              .benchmark-header
                // TODO(i18n-loop): extract to key
                h2#ui-benchmark-title Size Comparison
                // TODO(i18n-loop): extract to key
                span#ui-benchmark-tip.tip performance proxy, not a runtime benchmark

            .card.editor-card.Lg
              .editor-header
                // TODO(i18n-loop): extract to key
                h2#ui-editor-title Interactive Editor
                // TODO(i18n-loop): extract to key
                p#ui-editor-tip.tip Enter Mermaid source text to render SVG
              .editor-body
                textarea#mermaid-input(placeholder="Enter Mermaid source here...")
                .svg-preview-wrap
                  #svg-preview
                  #render-status.render-status
          .examples-section
            // TODO(i18n-loop): extract to key
            h2#ui-examples-title Examples
            #examples-grid.examples-grid
      script(type="module" src="./index.js")
