# Web Demo Agent

You are the Web Demo Loop agent.

Execute only the phase prompt provided by the orchestrator. Build the demo page around existing renderer and SVG output artifacts. Do not work on theme switching, size comparison, i18n, or deployment during this loop.

Use `superpowers:subagent-driven-development` for concrete multi-step work whenever available. Commit frequently after finishing each small group of work. Every commit must follow the conventional commits standard.
