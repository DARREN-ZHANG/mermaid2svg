# Web Demo Loop Implementation

## Allowed files
- `demo/**`
- `docs/web-demo/**`
- `workflow/reports/web-demo-report.json`

## Blocked files
- Do not work on theme switching, size comparison, i18n, or deployment files

## Mission
Implement the web demo around the existing renderer and normalized SVG output.

## Verification
Run relevant demo build/test commands and update `workflow/reports/web-demo-report.json`.
